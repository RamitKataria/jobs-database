import React from "react";
import ReactDom from "react-dom";

// TODO: link to admin and user
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


ReactDom.render(<App />, document.getElementById("root"));